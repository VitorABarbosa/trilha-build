diff --git a/apps/web/app/Guards/MFAGuard.tsx b/apps/web/app/Guards/MFAGuard.tsx
index 96f8114..d7c310b 100644
--- a/apps/web/app/Guards/MFAGuard.tsx
+++ b/apps/web/app/Guards/MFAGuard.tsx
@@ -28,10 +28,10 @@ export const MFAGuard = ({ children, mode }: MFAGuardProps) => {
   const redirectPath = resolvePostAuthRedirectPath({ pathname: lastEntry?.pathname });
 
   useEffect(() => {
-    if (mode !== "auth" || !currentUser || shouldVerifyMFA) return;
+    if (mode !== "auth" || !currentUser || shouldVerifyMFA || requiresPasswordChange) return;
 
     navigate(redirectPath, { replace: true });
-  }, [currentUser, mode, navigate, redirectPath, shouldVerifyMFA]);
+  }, [currentUser, mode, navigate, redirectPath, shouldVerifyMFA, requiresPasswordChange]);
 
   if (isLoading) {
     return null;
